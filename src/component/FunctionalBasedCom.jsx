import {View, Text} from 'react-native';
import React from 'react';

// Functional based Component First letter Caps
// Component is a show on UI is all things are components.
// if same module import the multiple part then use curly brase {}.
// component create for resuablity. WRITE ONE TIME USE ANYWHERE.

const FunctionalBasedCom = () => {
  return (
    <View>
      <Text>FunctionalBasedCom</Text>
    </View>
  );
};

export default FunctionalBasedCom;
